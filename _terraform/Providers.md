The providers defined in your [[Provider Block]] will have their plugin code/contents stored in the `.terraform/providers` when [[terraform init]] is called on a self-hosted/locally run terraform service. 